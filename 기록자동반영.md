# 시트 기록 자동 반영

GitHub 서버가 원본 Excel을 읽고, 변경된 개인·팀 기록을 홈페이지에 반영합니다. 운영자의 PC나 Codex를 켜 둘 필요가 없습니다. 구글 로그인이나 서비스 계정 없이 기존 공유 링크로 파일을 읽습니다. 원본 링크와 파일 식별자는 홈페이지와 공개 코드에 넣지 않습니다.

## 실행 시각

- 첫 예약 확인: **2026년 9월 12일 토요일 22:00 한국시간**
- 이후: **매일 22:00 한국시간**, 24시간 간격
- GitHub의 UTC 설정: `0 13 * * *`
- 유튜브 조회는 기존대로 매시 17분에 실행하며, 이때 시트를 읽지 않습니다.
- 원본에 변경이 없으면 기록 변환·커밋·해당 실행의 배포를 생략합니다.

GitHub 예약 실행은 혼잡하면 지연될 수 있습니다. 22시 확인 이후 저장된 변경은 다음 날 확인 때 반영됩니다. 공개 저장소에 60일 동안 활동이 없으면 예약 실행이 비활성화될 수 있습니다. [예약 실행 안내](https://docs.github.com/en/actions/reference/workflows-and-actions/events-that-trigger-workflows#schedule)

## 연결 설정

현재 원본은 Google Sheets 화면에서 열리는 **Drive의 Excel 파일**입니다. 링크가 있는 사람이 로그인 없이 다운로드할 수 있는 기존 공유 상태를 사용합니다. 네이티브 Google Sheets로 바꾸거나 Google Cloud, 서비스 계정, Apps Script를 설정할 필요가 없습니다.

GitHub 저장소의 **Settings → Secrets and variables → Actions**에서 `FCSA_DRIVE_FILE_ID` 하나로 파일 식별자를 관리합니다. 실제 값은 문서·소스 코드·홈페이지·실행 로그에 표시하지 않습니다. 이 설정은 원본 공유 권한을 바꾸지 않습니다. 원본을 나중에 ‘제한됨’으로 변경하거나 다운로드를 차단하면 자동 조회는 실패합니다.

**Actions → Update videos and publish FCSA → Run workflow**에서 기본 브랜치의 `sync_records`를 선택하면 즉시 확인할 수 있습니다. 수동 실행은 최초 예약 날짜 전에도 가능합니다. 원본에 변경이 없으면 `Unchanged`로 끝나며, 기록 파일이나 홈페이지를 다시 배포하지 않습니다.

## 반영 범위와 보존

- 선수 명단, 등번호·포지션, 개인 출전·득점·도움, 날짜별 팀원 기록, 시트의 팀 누적 전적·득실점·상대별 전적을 반영합니다.
- 인스타그램의 경기별 상대·최종 점수는 별도 자료입니다. 이번 자동화는 인스타그램을 읽거나 해당 자료를 덮어쓰지 않습니다. 용병 득점이 포함된 팀 득점을 팀원 개인 득점에 억지로 맞추지 않습니다.
- 매일 원본 파일을 한 번 내려받아 SHA-256으로 이전 내용과 비교합니다. 파일 내용이 같으면 기록 변환·커밋·배포를 생략합니다. 원본 식별자가 없거나 잘못되면 조회 전에 중단합니다.
- HTTPS로 확인된 Google 다운로드 호스트만 사용하고 응답 크기·XLSX 압축 구조를 검사합니다. 로그인 화면이나 확인 페이지를 Excel로 처리하지 않습니다. 필수 시트·헤더·날짜·숫자·수식 계산값 검증에 실패하면 기존 기록을 유지합니다.
- 다운로드 응답이 유효한 `Last-Modified`를 제공하면 원본 기준 시각으로 기록합니다. 제공하지 않으면 원본 수정일을 추정하지 않고 홈페이지 반영일만 표시합니다.
- 원본 Excel과 비고의 개인 메모는 커밋·배포하지 않습니다. 검증된 `data/records.js`만 자동 커밋하고 배포하므로 다음 유튜브 갱신 때 예전 기록으로 돌아가지 않습니다.
- 네트워크 일시 오류는 한 실행 안에서 제한적으로 재시도합니다. 계속 실패하면 기존 사이트를 유지하고 Actions에 실패가 남습니다. 다음 예약 또는 수동 실행에서 다시 확인할 수 있습니다.
- 새 기록이 있을 때 유튜브 조회만 실패하면, 현재 공개된 영상 목록을 검증해 기존 확인 시각 그대로 유지하고 새 기록을 배포합니다. 그 목록도 읽지 못하면 배포를 중단합니다. 이미 저장된 새 기록은 다음 정상 배포에서 반영됩니다.

사이트를 새로 열거나 새로고침하면 마지막 배포의 기록을 보여줍니다. 이미 열려 있는 선수·기록 화면을 강제로 새로고침하지 않습니다.

현재 변환기는 기존 **2026년 양식**을 검증합니다. 새해에 시트 이름·연도·구조가 바뀌면 사이트의 변환기를 갱신해야 합니다. 원본 양식을 홈페이지에 맞춰 바꿀 필요는 없습니다.

## 로컬 검증

```powershell
python -m pip install -r tools/requirements-records.txt
python -B -m unittest discover -s tools -p 'test_*.py'
```

검증은 합성 파일과 모의 응답으로 실행하며 실제 원본을 변경하지 않습니다. `FCSA_DRIVE_FILE_ID` 환경변수를 설정한 환경에서는 `python tools/sync_records.py`로 같은 읽기 경로를 실행할 수 있습니다. 파일 식별자를 명령 인수나 공개 파일에 직접 적지 않습니다.

수동 다운로드 파일을 변환하는 기존 `tools/import_records.py`도 계속 사용할 수 있습니다. 실제 마지막 수정 시각을 `--modified-at`으로 전달해야 합니다.
