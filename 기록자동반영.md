# 시트 기록 자동 반영

GitHub 서버가 원본 Excel을 읽고, 변경된 개인·팀 기록을 홈페이지에 반영합니다. 운영자의 PC나 Codex를 켜 둘 필요가 없습니다. 아래 인증 설정을 완료해야 실제 원본을 가져올 수 있습니다.

## 실행 시각

- 첫 예약 확인: **2026년 9월 12일 토요일 22:00 한국시간**
- 이후: **매일 22:00 한국시간**, 24시간 간격
- GitHub의 UTC 설정: `0 13 * * *`
- 유튜브 조회는 기존대로 매시 17분에 실행하며, 이때 시트를 읽지 않습니다.
- 원본에 변경이 없으면 기록 변환·커밋·해당 실행의 배포를 생략합니다.

GitHub 예약 실행은 혼잡하면 지연될 수 있습니다. 22시 확인 이후 저장된 변경은 다음 날 확인 때 반영됩니다. 공개 저장소에 60일 동안 활동이 없으면 예약 실행이 비활성화될 수 있습니다. [예약 실행 안내](https://docs.github.com/en/actions/reference/workflows-and-actions/events-that-trigger-workflows#schedule)

## 한 번 필요한 연결

현재 원본은 Google Sheets 화면에서 열리는 **Drive의 Excel 파일**입니다. 네이티브 Google Sheets로 바꾸거나 기존 시트에 스크립트를 추가할 필요가 없습니다.

1. 팀이 관리하는 Google Cloud 프로젝트에서 **Google Drive API**를 사용 설정합니다.
2. 이 파일을 읽는 전용 서비스 계정을 만들고 JSON 키를 발급합니다. 프로젝트 편집자·소유자 역할이나 도메인 전체 위임은 필요하지 않습니다. 파일 접근은 다음 공유 설정으로 부여합니다.
3. 원본 소유자가 해당 서비스 계정 이메일에 **뷰어(보기)** 권한을 부여합니다. 다운로드가 허용되어야 합니다. 일반 액세스는 **제한됨**으로 둘 수 있으며, 파일의 내용·서식·수식을 바꾸지 않습니다.
4. GitHub 저장소의 **Settings → Secrets and variables → Actions → New repository secret**에서 아래 두 항목을 등록합니다.

| Secret 이름 | 값 |
|---|---|
| `FCSA_DRIVE_FILE_ID` | 기존 원본 파일의 식별자 |
| `FCSA_GOOGLE_SERVICE_ACCOUNT_JSON` | 위 전용 서비스 계정의 JSON 키 전체 |

실제 파일 링크·식별자·키는 이 문서나 소스 코드에 넣지 않습니다. JSON 키는 비밀번호처럼 취급하고 채팅이나 커밋에 붙여 넣지 않습니다. 인증은 Google의 `google-auth` 라이브러리가 처리하며, 요청 범위는 `drive.readonly`입니다. 다른 팀 문서를 이 서비스 계정에 공유할 필요가 없습니다.

5. **Actions → Update videos and publish FCSA → Run workflow**에서 기본 브랜치의 `sync_records`를 선택하여 한 번 실행합니다. 수동 실행은 최초 예약 날짜 전에도 가능합니다. 원본을 수정하지 않고 접근·변경 확인·검증이 되는지 확인할 수 있습니다.

두 Secret이 없거나 읽기 권한이 없으면 기록 동기화는 명확히 실패합니다. 인증 미설정을 정상 갱신으로 표시하지 않습니다. 원본 공유 상태는 소유자가 직접 확인해야 하며, 이 프로그램은 공유 권한을 변경하지 않습니다.

## 반영 범위와 보존

- 선수 명단, 등번호·포지션, 개인 출전·득점·도움, 날짜별 팀원 기록, 시트의 팀 누적 전적·득실점·상대별 전적을 반영합니다.
- 인스타그램의 경기별 상대·최종 점수는 별도 자료입니다. 이번 자동화는 인스타그램을 읽거나 해당 자료를 덮어쓰지 않습니다. 용병 득점이 포함된 팀 득점을 팀원 개인 득점에 억지로 맞추지 않습니다.
- 원본의 수정 정보와 해시를 먼저 확인합니다. SHA-256이 같으면 원본 파일 다운로드를 생략합니다. 서버가 SHA-256을 제공하지 않는 경우에는 다운로드한 내용의 해시로 비교합니다.
- 변경된 파일은 다운로드 전후 버전과 해시를 확인한 뒤 기존 변환기로 검증합니다. 필수 시트·헤더·날짜·숫자·수식 계산값이 잘못되었거나 다운로드 중 원본이 바뀌면 기존 기록을 유지합니다.
- 원본 Excel과 비고의 개인 메모는 커밋·배포하지 않습니다. 검증된 `data/records.js`만 자동 커밋하고 배포하므로 다음 유튜브 갱신 때 예전 기록으로 돌아가지 않습니다.
- 네트워크 일시 오류는 한 실행 안에서 제한적으로 재시도합니다. 계속 실패하면 기존 사이트를 유지하고 Actions에 실패가 남습니다. 다음 예약 또는 수동 실행에서 다시 확인할 수 있습니다.
- 새 기록이 있을 때 유튜브 조회만 실패하면, 현재 공개된 영상 목록을 검증해 기존 확인 시각 그대로 유지하고 새 기록을 배포합니다. 그 목록도 읽지 못하면 배포를 중단합니다. 이미 저장된 새 기록은 다음 정상 배포에서 반영됩니다.

사이트를 새로 열거나 새로고침하면 마지막 배포의 기록을 보여줍니다. 이미 열려 있는 선수·기록 화면을 강제로 새로고침하지 않습니다.

현재 변환기는 기존 **2026년 양식**을 검증합니다. 새해에 시트 이름·연도·구조가 바뀌면 사이트의 변환기를 갱신해야 합니다. 원본 양식을 홈페이지에 맞춰 바꿀 필요는 없습니다.

## 로컬 검증

```powershell
python -m pip install -r tools/requirements-records.txt
python -B -m unittest discover -s tools -p 'test_*.py'
```

검증은 합성 파일과 모의 응답으로 실행하며 실제 원본을 변경하지 않습니다. 실제 인증을 설정한 환경에서는 `python tools/sync_records.py`로 같은 읽기 경로를 실행할 수 있습니다. 필요한 두 환경변수의 이름은 위 Secret과 같습니다. 인증값을 명령 인수에 직접 적지 않습니다.

수동 다운로드 파일을 변환하는 기존 `tools/import_records.py`도 계속 사용할 수 있습니다. 실제 마지막 수정 시각을 `--modified-at`으로 전달해야 합니다.
